#!/usr/bin/env bash
set -euo pipefail

if [ "$#" -lt 1 ]; then
  printf 'Usage: screenshot-thumbnail PNG [floating-screenshot options]\n' >&2
  exit 2
fi

_png="$1"
shift
exec "$(dirname "$0")/floating-screenshot" --thumbnail "$_png" "$@"
