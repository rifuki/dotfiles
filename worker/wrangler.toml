name = "dotfiles-worker"
main = "build/worker/shim.mjs"
compatibility_date = "2025-12-01"

[build]
command = "cargo install -q worker-build && worker-build --release"

[[routes]]
pattern = "dotfiles.rifuki.dev/*"
zone_name = "rifuki.dev"
